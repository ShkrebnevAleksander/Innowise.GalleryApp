<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Gallery.ViewModels"
             x:DataType="viewmodels:FavoritesViewModel"
             x:Class="Gallery.Views.FavoritesPage"
             Title="Favorites">

    <Grid>
        <CollectionView ItemsSource="{Binding FavoritePhotos}"
                        SelectionMode="None">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="2"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:PhotoViewModel">

                    <Grid Padding="5" BackgroundColor="Transparent">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnPhotoTapped"/>
                        </Grid.GestureRecognizers>
                        <Image Aspect="AspectFill"
                               Source="{Binding Photo.Urls.Thumb}"
                               HeightRequest="150" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <ActivityIndicator IsRunning="{Binding IsLoading}"
                             HorizontalOptions="Center"
                             VerticalOptions="Center"
                             Color="Orange"/>
    </Grid>
</ContentPage>